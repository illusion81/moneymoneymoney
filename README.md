# moneymoneymoney

A Flutter app.

## Setup

Requires the [Flutter SDK](https://docs.flutter.dev/get-started/install) (developed against Flutter 3.41, Dart SDK `^3.11.0`).

```sh
flutter pub get      # install dependencies
flutter devices      # list available targets
flutter run          # run the app (add -d <device> to pick a target)
```

## Common commands

```sh
flutter test         # run tests
flutter analyze      # static analysis / lints
flutter build apk    # release build (also: ios, macos, linux, windows, web)
```

---

# moneymoneymoney

一个 Flutter 应用。

## 安装配置

需要 [Flutter SDK](https://docs.flutter.dev/get-started/install)（基于 Flutter 3.41 开发，Dart SDK `^3.11.0`）。

```sh
flutter pub get      # 安装依赖
flutter devices      # 列出可用的目标设备
flutter run          # 运行应用（加 -d <device> 可指定目标设备）
```

## 常用命令

```sh
flutter test         # 运行测试
flutter analyze      # 静态分析 / 代码检查
flutter build apk    # 发布构建（也支持：ios、macos、linux、windows、web）
```
